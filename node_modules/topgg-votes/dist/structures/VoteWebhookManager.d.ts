import { VoteWebhookManagerOptions } from 'types';
export declare class VoteWebhookManager {
    private _client;
    private _port;
    private _path;
    private _authorization;
    constructor(options: VoteWebhookManagerOptions);
    setPort(port: number | null): this;
    setPath(path: string | null): this;
    startWebhookServer(): void;
}
