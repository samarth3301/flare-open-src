/// <reference types="node" />
import { BotInfo, ShortUser, UserInfo } from '@top-gg/sdk';
import { EventEmitter } from 'events';
import { VoteClientConfigOptions } from 'types';
export declare class VoteClient extends EventEmitter {
    private _authToken;
    private _webhookOptions;
    constructor(options?: VoteClientConfigOptions);
    setToken(token: string): this;
    setWebhookPath(path: string): this;
    setWebhookPort(port: number): this;
    setWebhookAuthorization(authorization: string): this;
    hasToken(): boolean;
    getVotes(): Promise<ShortUser[]>;
    hasVoted(userId: string): Promise<boolean>;
    getBot(botId: string): Promise<BotInfo>;
    getUser(userId: string): Promise<UserInfo>;
}
